## InterviewAgent — 基于 Eino 框架的 AI 模拟面试系统

技术栈: Go 1.22 + Eino + DeepSeek API + Milvus + Bleve BM25 + Redis + MySQL + Gin + WebSocket + MCP + Docker

**项目概述**: 独立设计并实现的全栈 AI 面试平台，覆盖 JD 解析、简历匹配、混合检索出题、追问式多轮面试、四维评估和复习计划生成六个环节。后端基于 Eino 框架的有向无环图编排多 Agent 协作，前端支持 Web、微信小程序接入方式。

**图编排与中断恢复**: 构建 6 节点固定拓扑（JD解析→简历匹配→出题规划→面试官提问→评估→复习计划），通过分支条件路由控制追问、下一题、结束三条路径。每题提问后挂起图执行，将面试状态（题号、对话历史、评分、难度）序列化后存入 Redis 检查点，用户回答后恢复执行，支持断线或换设备后 1 小时内精确恢复到中断前的题目和对话上下文。

**上下文管理**: 针对长对话中 token 膨胀和关键信息丢失问题，设计了窗口分配、记忆分层、压缩策略、上下文编排、溢出降级和运行时监控六层机制。
- *窗口分配*: 将大模型上下文窗口视为有限预算，区分不可丢弃内容（如系统提示词）和可压缩内容（如历史对话），分别走预留和消费两个通道。定义 6 种场景配置（闲聊、面试提问、面试评估等），按场景差异化分配系统提示词、工作记忆和参考文档的配额。
- *记忆分层*: 三层模型——工作记忆层每次调用时从 Redis 拉取近期消息按预算组装；短期记忆基于 Redis 列表维护最近 60 条消息的滑动窗口；长期记忆基于 MySQL 归档全量对话、摘要和用户薄弱领域画像。对话超过阈值时自动生成摘要注入系统提示词。
- *压缩策略*: 四种策略按场景组合——A 策略（滑动窗口+渐进摘要）用于面试提问，保留最近 3 轮原文，中间轮次压缩为问答摘要，实测 token 占用降低约 30%；B 策略（结构化提取）用于评估，将原始对话转为结构化字段；C 策略（大模型摘要）异步生成自然语言摘要；D 策略（推理步骤压缩）对多步推理链进行压缩，保留最近 3 步原文，旧步骤提取为摘要。同时从对话中识别关键实体注入系统提示词头部，确保不被滑动窗口丢弃。
- *上下文编排*: 面试六个阶段各自独立分配上下文配置，阶段完成后清理不再需要的原始文本（如 JD 原文、简历原文），对话超过阈值时返回压缩摘要加最近原文的组合。
- *溢出降级*: 降级策略——裁剪超限工具返回→压缩对话历史→检索文档减半→简化系统指令→新建新会话，逐级执行，满足预算即停止。
- *运行时监控*: 每次上下文构建后自动上报各组件 token 消耗，超过 80% 触发警告日志，超过 95% 触发严重告警，暴露聚合指标供外部查询。

**混合检索出题**: Milvus 语义检索与 Bleve BM25 关键词检索双通道混合管道，各取 Top-3 候选后由大模型进行第二阶语义重排序取 Top-3 注入面试官提示词。重排序失败时自动降级为截断，不阻塞主流程。

**可插拔技能系统**: 基于统一接口（名称、匹配判定、处理逻辑、新建会话）实现 8 个技能模块——快速测验、知识讲解、项目亮点提炼、技术对比、算法练习、系统设计、行为面试 STAR 法、技术测验。每个技能持有独立多轮会话状态，经 Redis 检查点持久化。

**动态难度调整**: 基于状态机实现三级难度自适应，统计连续答对与答错次数，达到阈值自动升降级并重置计数器，影响面试官提示词和出题配比。
